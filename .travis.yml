language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install -e .
  - pip install coveralls

script:
  - coverage run --source=src -m pytest
    
after_sucess:
  - coverage report -m
  - coveralls